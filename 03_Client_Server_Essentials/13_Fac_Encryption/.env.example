DATABASE_URL=postgres://postgres:root@localhost:5432/adventure
PORT=4000

# Bcrypt configuration
# The salt rounds determine how many times the password is hashed
# Higher = more secure but slower (10-12 is recommended for production)
BCRYPT_SALT_ROUNDS=10
