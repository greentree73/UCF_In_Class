# REST Intro API (HackHaven)

## Run

```bash
npm install
npm run dev
```

## Endpoint

- `GET /api/post`

## Example

```bash
curl http://localhost:3001/api/post
```

This returns the full post object defined in `app.ts`.
