import { gql } from '@apollo/client';

export const GET_GREETING = gql`
  # TODO: Complete this query to declare a $name variable and pass it
  # into greeting(name: $name).
  query GetGreeting {
    __typename
  }
`;
