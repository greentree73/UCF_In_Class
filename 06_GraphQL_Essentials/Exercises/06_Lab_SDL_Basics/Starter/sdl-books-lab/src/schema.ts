export const typeDefs = `
  # TODO: Step 1 - Define a Book type that maps to src/models/BOOKS.ts
  # Required fields: id, title, author, genre, publishedYear, inStock, createdAt

  # TODO: Step 2 - Replace this placeholder Query type
  type Query {
    placeholder: String
  }

  # TODO: Step 3 - Add CreateBookInput and Mutation type with createBook(input: CreateBookInput!): Book!
  type Mutation {
    placeholder: String
  }
`;
