import GreetingForm from './components/GreetingForm';

function App() {
  return (
    <main className="app-shell">
      <h1>🛰️ Apollo Splashdown: Client Authentication</h1>
      <p>Register, login, and fetch authenticated user data with Apollo Client + JWT.</p>
      <GreetingForm />
    </main>
  );
}

export default App;
